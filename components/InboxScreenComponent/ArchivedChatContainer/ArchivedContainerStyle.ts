import { StyleSheet } from "react-native";

const ArchivedContainerStyle = StyleSheet.create({
  container: {
    padding: 10,
    backgroundColor: "lightgray",
    marginVertical: 10,
    marginHorizontal: 10,
    borderRadius: 10,
  },

  text: { fontSize: 16, fontWeight: "bold", color: "grey" },
});


export default ArchivedContainerStyle;